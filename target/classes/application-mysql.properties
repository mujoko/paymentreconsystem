# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost/payment}
spring.datasource.username=${MYSQL_USER:payment}
spring.datasource.password=${MYSQL_PASS:payment}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always
